version: "3.9"

services:
  web:
    build: ./backend
    ports:
      - "8100:8100"
    volumes:
      - ./frontend:/app/frontend
    environment:
      - DATABASE_URL=postgresql://traceability_user:TraceDB2024@92.120.147.79:5432/dummy_traceability
    restart: unless-stopped